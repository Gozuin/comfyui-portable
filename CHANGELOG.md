# 更新记录

## v0.2.0 - 2026-09-25

这次更新围绕一个目标：让 AI 可以更轻松、更放心地直接调用 ComfyUI，不用为了一个技能再搭一层 MCP，也不用把某台电脑的模型名和路径写进公开仓库。

### 修好了什么

- 预演不再偷偷上传文件。以前 `--dry-run` 带参考图时仍可能修改 ComfyUI 的输入目录，现在只生成计划，不会上传、提交或卸载模型。
- 参考文件不再互相覆盖。不同任务、不同目录里的同名文件会放进任务专属子目录，并使用唯一文件名；默认不再覆盖服务器上的旧文件。
- `--json` 现在真的只输出 JSON。以前开头混有 `Saved: ...`，脚本无法直接解析；现在机器模式只向 stdout 输出一个完整 JSON，日志改为 stderr。
- 配置不再“看起来成功其实不能用”。缺模型、模型列表为空、缺节点或未在线验证时，会明确保存为 `draft`；只有真正验证通过的配置才是 `ready`，`run` 默认拒绝 draft。
- 模型识别不再把普通下拉框误当成模型。像 `CLIPLoader.type` 这类枚举现在只做参数校验，不会被登记成模型文件。
- 错误绑定不再静默失效。字符串绑定、残缺列表、未知角色、没有实际用途的参数都会直接报错，避免 AI 以为提示词已经写入工作流，实际却完全没变。
- 不再把已经开始执行的任务误报为提交失败。只要 ComfyUI 返回了 `prompt_id`，任务就会被保留；部分分支报错也会记录为“已接受但有节点错误”。
- 下载结果不再撞名覆盖。即使服务器返回多个同名文件，也会按节点和输出序号保存；下载先写临时文件，成功后才替换目标文件。
- 默认不再请求全局释放模型。避免一次普通生成影响同一台 ComfyUI 上的其他任务，确有需要时再做显式处理。
- 图连接参数不会被随意覆盖。参数绑定到其他节点的连线时，默认拒绝用固定数值把它冲掉，防止工作流结构被改坏。
- 工作流和描述文件修改后会被发现。配置保存了文件指纹和节点 schema 指纹，漂移时会提示重新检查或配置，而不是继续使用过期绑定。

### 新增了什么

- `setup`、`doctor`、`run`、`submit`、`status`、`wait`、`fetch` 的统一机器结果合同。
- 稳定的错误码、状态值和退出码，AI 可以据此判断是改输入、修配置、排查网络还是停止重试。
- 任务清单。`submit` 会立即保存任务 ID、`prompt_id`、上传记录和请求摘要；客户端中断后，可以用 `status`、`wait`、`fetch` 继续同一个任务。
- 更强的描述文件。支持 `task_type`、参数类型与枚举、多个输入绑定，以及显式扩展自定义模型加载器。
- SDXL 双文本编码支持。`text_g` 与 `text_l` 会被当成同一文本编码器的双通道，不再错误地理解为“正向”和“负向”。
- 输出保存策略。默认按节点、输出序号和原文件名组合保存，避免多输出、多目录同名时互相覆盖。
- 更安全的上传计划。预演会列出准备读取的文件、目标节点和目标输入，但不会读取后写入 ComfyUI。
- 更准确的文档和 `SKILL.md`。删除了“自动寻找任意服务器”“自动保证模型兼容”等过强表述，明确哪些必须由用户确认。
- 20 项回归测试，覆盖安全预演、模型与节点校验、绑定边界、JSON 合同、任务恢复和防覆盖。

### 仍然需要知道的边界

- 这个版本坚持直接 HTTP，不增加 MCP。
- 只支持 ComfyUI API 格式工作流，不负责把普通 UI 工作流自动转换。
- 换电脑后仍然需要重新 `setup`。
- 自动识别主要覆盖常见节点；复杂工作流应提供 descriptor。
- 视频、特定模型架构和真实显存表现仍需要对应 ComfyUI 环境的真实 smoke 验证。
