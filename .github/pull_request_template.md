## Summary

Describe the problem and the change.

## Verification

- [ ] `python -m unittest discover -s tests -v`
- [ ] `python -m compileall -q scripts tests`
- [ ] Workflow descriptor validated with `inspect` or `doctor`

## Compatibility

Note any command, config schema, or workflow compatibility changes.
